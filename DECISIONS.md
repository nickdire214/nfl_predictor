# Decisions Log

| Date | Component | Change | Reason | Evidence |
| --- | --- | --- | --- | --- |
| 2026-06-10 | Ingestion | Use nflreadpy instead of deprecated nfl_data_py; convert Polars→pandas at ingestion boundary | nfl_data_py officially deprecated, no further updates | nflverse README |
| 2026-06-10 | Ingestion | Verified Odds API key works pre-season; quota at 425 remaining / 75 used after this check | Confirm API access and quota headroom before building ingestion against it | scripts/check_odds_api.py output (2 calls, both HTTP 200) |
| 2026-06-10 | Ingestion | Use seasons 2021-2025 (17-game era) for player_stats, snap_counts, schedules; store raw pulls as parquet in data/raw/ | 2020 excluded as a COVID-affected, anomalous (16-game) season; parquet is compact and preserves dtypes for downstream pandas use | src/ingestion/stats.py output: player_stats (94845, 115) 3.20MB, snap_counts (132616, 16) 0.99MB, schedules (1424, 46) 0.13MB |
| 2026-06-10 | Ingestion | Raw layer complete: five datasets (player_stats, snap_counts, schedules, injuries, players crosswalk), all sourced from nflverse via nflreadpy; Odds API reserved for player props only since schedules already carry historical game lines (spread/total/moneyline) | Avoids redundant Odds API calls/quota usage for data nflverse already provides; players table gives gsis_id/pfr_id/espn_id/etc. crosswalk for joining across sources | src/ingestion/injuries.py output: injuries (29151, 17) 0.47MB, seasons 2021-2025; players (25041, 39) 3.23MB |
| 2026-06-10 | Features | player_stats.game_id is null for all of 2021 and 2024 (and a few stragglers in 2022/2025), so joins between schedules and player_stats use (season, week, team, player_id) instead of game_id — adopted as the standard join key going forward | game_id-based join only matched 60% of starter rows (1697/2848); (season, week, team, player_id) is complete and unique in both tables and matched 98.3% (2799/2848) | src/features/engineer.py build_qb_base_table() diagnostics |
| 2026-06-11 | Models | Adopt in-season rolling calibration via shrinkage (offset = mean_error * n/(n+n0), n0=150) on top of the Ridge passing-yards model | Walk-forward grid search over 4 seasons (2022-2025) showed shrinkage halves summed |bias| (19.12 -> 10.90) at ~zero MAE cost (234.41 -> 234.33), and the choice is insensitive across n0 in [100, 300] — pre-registered acceptance criteria met | scripts/test_rolling_calibration_v2.py output; src/models/calibrate.py RollingCalibrator validated by scripts/test_calibrate.py (4/4 tests pass, incl. exact replay match of 2025 MAE 57.73 / bias 3.72) |
| 2026-06-11 | Models | Probability layer (src/models/predict.py) built on the empirical residual CDF (Option B): a pooled, mean-centered out-of-sample residual distribution is added to each calibrated point prediction to derive prob_over and predict_quantiles | Constant variance validated by flat residual-std buckets across prediction, attempts_l8, and games-into-season groups; 2025 honest-coverage test landed within ~4.5pts of nominal at all five levels (location-shift caveat noted: mid-distribution coverage runs slightly high); residual pool mean-centered to avoid double-counting location already handled by RollingCalibrator | scripts/analyze_residuals.py output; scripts/build_residual_pool.py (models/qb_residual_pool.npy, n=2175, std=74.63, mean ~0); scripts/test_predict.py (5/5 tests pass) |
| 2026-06-11 | Policy | Preseason excluded entirely from all modeling: no preseason stats in features, training data, calibration, or evaluation. Odds API preseason markets may optionally be used once, in August, as a mechanics-only dry run of odds.py — never as model input | nflverse data already contains REG + playoffs only, so this codifies existing behavior as explicit policy rather than an accident of the source data | player_stats.season_type unique values = ['REG','POST']; schedules.game_type unique values = ['REG','WC','DIV','CON','SB'] — no 'PRE' present in either |
| 2026-06-11 | Policy | 2026 season begins as paper-trading: every QB with a posted line gets a logged prediction + P(over), with no bet-flagging. Edge threshold deliberately deferred until ~4-6 weeks of live calibration data exist; once chosen, it is frozen for the rest of the season. Prediction logs are immutable once written | No live betting decisions until the probability layer's real-world performance can be observed under live odds; freezing the threshold mid-season and keeping logs immutable preserves an honest, unbiased record for evaluating it | n/a — forward-looking policy, to be enforced when the 2026 logging pipeline is built |
| 2026-06-11 | Features | Add build_prediction_features(season, week, starters=None, line_overrides=None) to src/features/engineer.py: builds upcoming-week feature rows by appending NaN-stat target rows to the historical base table and reusing the same rolling-feature computation as training (extracted into _compute_rolling_features) | Reuses, rather than duplicates, the leakage-safe rolling logic validated in Step 8; replay equivalence confirms it reproduces training-time features exactly, and a self-leakage check confirms a week's own stats have zero influence on its own features | scripts/test_prediction_features.py (3/3 tests pass): (a) 2025 weeks 5 and 14, all 28 rows each, every FEATURE_COLS column matches qb_matrix.parquet exactly; (b) 2025 week 10 features identical with vs without that week's own player_stats rows; (c) 2025 week 23 (no schedule) runs without error, returns (0, 53) with matching columns |
| 2026-06-11 | Models | Weekly prediction runner (src/models/predict.py: resolve_starters + run_week, runnable via `python -m src.models.predict --season --week`): full-history retraining of the final pipeline on all rows strictly before (season, week); a separate season-prior pipeline is replayed week-by-week through a fresh RollingCalibrator(n0=150) (same pattern as build_residual_pool.py) to set the calibrator offset for the target week; starters default to each team's most recent prior starter with optional CSV overrides resolved by exact QB display_name match (errors on unknown/ambiguous names); predictions are saved to data/predictions/{season}_w{week:02d}_qb_pass_yards.parquet and the file is treated as immutable — re-running without force raises FileExistsError | Keeps the live model as fresh as possible (full history including the current season) while keeping calibration honest (only data prior to each replayed week informs that week's error); name-based override resolution avoids hand-typing gsis_ids; immutable logs are required for the 2026 paper-trading policy | Dry run for 2025 week 8: 26/26 non-bye teams predicted (sorted by predicted yards desc, top = Daniel Jones 277.99, calibrator_offset=3.997); starter resolution table printed for all 32 teams; saved to data/predictions/2025_w08_qb_pass_yards.parquet; re-run without --force raised FileExistsError as expected |
| 2026-06-11 | Models | Weekly grader (src/models/evaluate.py: grade_week, runnable via `python -m src.models.evaluate --season --week`): grades a saved prediction file against qb_matrix actuals, classifying each row GRADED / WRONG_STARTER / NO_GAME; tracks starter accuracy (graded / played games) as a first-class operational metric alongside MAE/bias; buckets each GRADED actual against its predicted quantiles (below_p10 ... above_p90) and compares the observed distribution to the expected 10/15/25/25/15/10% as a running calibration check; grades saved to *_grades.parquet under the same immutability rule as predictions | Starter misses (wrong/backup QB started) need to be visible and excluded from accuracy stats, not silently absorbed into MAE; the quantile-bucket distribution is the cheapest week-over-week signal that the residual-pool-based probability layer is still well-calibrated as live data accumulates | 2025 week 8: 26 rows (GRADED=22, WRONG_STARTER=2 [ATL: Penix Jr. predicted vs Cousins started; BAL: Cooper Rush predicted vs Tyler Huntley started], NO_GAME=2 [CAR, WAS — no qb_matrix row]); starter accuracy 22/24=0.917; MAE=50.50, bias=3.03; quantile-bucket distribution printed vs expected; saved to data/predictions/2025_w08_qb_pass_yards_grades.parquet |
| 2026-06-11 | Ingestion | Odds layer part 1 (src/ingestion/odds.py): schedules.parquet schedule ingestion extended to 2021-2026 (SCHEDULE_SEASONS, separate from the 2021-2025 SEASONS used for player_stats/snap_counts); TEAM_NAME_MAP (Odds API full names -> nflverse abbrs, all 32 teams, asserted against schedules' team set); fetch_events() pulls the free events endpoint; match_events_to_schedule() is schedule-authoritative, joining events to schedules.parquet on (home_team, away_team, gameday_et) and printing mismatches in both directions; fetch_game_lines(snapshot_label) bulk-pulls spreads+totals (region=us) in one call (2 credits regardless of event count), one row per (event_id, bookmaker), saved to data/raw/odds/game_lines_{snapshot_label}.parquet; player-prop pulls remain deferred to the August preseason dry run | Schedule-authoritative matching on (teams, ET date) is more durable than relying on the Odds API's own event IDs across snapshots; a single bulk game-line call is far cheaper than per-event or per-player-prop pulls, so prop pulls stay deferred until closer to the season per the existing preseason policy | Ran 2026-06-11: fetch_events returned 75 events spanning 2026-09-09..2026-10-11 (season weeks 1-5); 75/75 matched a schedule row (100%); 3 week-5 schedule games (DET@ARI, CHI@GB, BAL@ATL, all 2026-10-11) had no matching event yet — those events aren't posted on the Odds API yet, a real data-availability gap rather than a matching bug; fetch_game_lines returned 260 rows across all 75 matched events and 9 bookmakers, saved to data/raw/odds/game_lines_2026wk1_test_2026-06-11.parquet; quota went from 425/75 to 423/77 remaining/used, confirming the 2-credit cost for markets=spreads,totals & regions=us |
| 2026-06-11 | Ingestion | Odds layer part 2 (src/ingestion/odds.py): consensus_game_lines(lines_df) collapses a snapshot to one row per event_id (median spread_home, median total, n_books); lines_to_overrides(consensus_df, matched_events) converts to the {team: (spread_line, total_line)} dict build_prediction_features expects, applying spread_line = -spread_home (Odds API home-team handicap, home favorite negative -> nflverse spread_line, home favorite positive) and printing a 3-game sanity check confirming the favorite gets the larger implied total. src/models/predict.py run_week gained line_overrides (pass-through to build_prediction_features) and label (appends _{label} to the output filename for non-canonical/test runs), both exposed via --label in the CLI; result rows now include team_implied_total | Median-across-books is a simple, robust consensus that's easy to audit via n_books; the spread sign conversion is the single highest-risk step in this chain (a silent flip would feed exactly-backwards game-script context into every QB's features), so it gets an explicit printed sanity check rather than a one-line trust-the-formula conversion; filename labels let test/paper-trading runs share the same code path as real weekly runs without risking the immutable logs | scripts/test_2026wk1_lines_to_predictions.py: loaded the saved 260-row snapshot, re-fetched events (free) to recover season/week/game_id, built consensus for 75 games and overrides for all 32 week-1 teams; 3-game sanity check (NE@SEA, SF@LA, ATL@PIT) all printed OK (favorite has larger implied total); run_week(2026, 1, line_overrides=..., label="june_test") produced 32 rows, all with non-NaN team_implied_total (0/32 missing), saved to data/predictions/2026_w01_qb_pass_yards_june_test.parquet; calibrator_offset=0.0 for all rows since 2026 has no completed weeks yet to replay |
| 2026-06-11 | Features | Opponent pass-defense features (DEFENSE_COLS: def_pass_yards_allowed_l8/std, def_pass_epa_allowed_l8/std) REJECTED for FEATURE_COLS per the pre-registered A/B decision rule (adopt only if mean MAE improves AND no season degrades by more than 0.3); columns remain computed in qb_matrix.parquet (for a possible future re-test) but excluded from FEATURE_COLS | Mean MAE delta across 2022-2025 was -0.03 (slight improvement), but 2022 alone degraded by +0.98, far over the 0.3 ceiling; deltas improved monotonically with training-fold size (+0.98 / -0.20 / -0.27 / -0.64), and arm B's standardized coefficients showed a large opposite-signed _l8/_std pair for pass EPA allowed (+2.50 / -7.36), suggesting an unstable contrast effect rather than a robust signal at current data volumes | scripts/ab_defense_features.py output: 0 rows dropped for missing def_pass_yards_allowed_l8 (already covered by preprocess_qb_matrix's passing_yards_l3 filter); per-season MAE_A/MAE_B 59.52/60.51 (2022), 58.67/58.47 (2023), 58.41/58.14 (2024), 57.73/57.09 (2025); eligible for ONE re-test after the 2026 season once every walk-forward fold has >=1,000 training rows, with criteria re-registered before that run |
| 2026-06-11 | Features | Extracted the rolling-feature pattern (shift-then-roll L3/L8 crossing seasons, shift-then-expanding _std resetting per season, games_into_season) into a generic src/features/rolling.py add_rolling_features(df, group_col, stat_cols, ...), used by both the QB path (engineer.py, group_col="qb_id") and a new receiving matrix (receiving.py, group_col="gsis_id"); receiving matrix built from the snap-based player-game spine (Step 24), with target_share/air_yards_share/wopr zero-filled on played-but-uninvolved rows (racr excluded, stays NaN) and 8 stats rolled (targets, receptions, receiving_yards, receiving_air_yards, offense_pct, target_share, air_yards_share, wopr) -> 24 rolling cols + games_into_season | Avoids duplicating the leakage-safe rolling logic for the receiving path; a byte-identical regression proof on the QB matrix gives confidence the refactor changed nothing about QB features before building the new receiving features on the same machinery | Refactor: qb_matrix.parquet regenerated and pandas.testing.assert_frame_equal against a pre-refactor copy passed (same shape (2799,57), same columns, identical values); test_rolling_features.py (6/6) and test_prediction_features.py (3/3) still pass. Receiving: receiving_matrix.parquet shape (30113, 57); scripts/test_receiving_features.py (6/6 pass): (a) shift correctness, (b) first-game all-NaN, (c) no cross-player bleed, (d) Ja'Marr Chase 2025 week 13 targets_l3 manual recompute over played weeks 8/9/11 (bye+missed-week indexing), (e) season-to-date reset, (f) a played-zero-target game is included as 0 in the next game's L3 (not skipped) |
| 2026-06-15 | Models | Receiving probability layer (the 27->32 evidence chain): heteroskedasticity discovered (residual std ~2x by volume and by position RB<TE<WR; pooled single-distribution produced negative p10 yardage and 0%/20% tail coverage in extreme quartiles); four architectures (pooled, pred-conditional sigma, position+pred-conditional sigma, quantile regression) tested under a pre-registered 5pp/20-cell coverage bar; point-mass-at-zero metric lesson (scalar coverage cannot read nominal inside a discrete lump at 0 -- affected only quantile-reg, confirmed by tie-aware deltas isolating exclusively to tie cells); all four failed the bar; registered floor invoked -> Brier tiebreak at product-realistic lines -> cond-pos (position+pred-conditional sigma) adopted. Documented blemish: bottom-quartile median coverage ~5.7pp hot under tie-aware scoring. Reliability tables show both finalists ~2pp hot on overs -- to be tracked in live grading, not pre-season-adjusted | Pooled single-distribution residuals were unusable as a product probability layer (negative yardage at p10, badly miscalibrated tails for high- and low-volume receivers); a pre-registered bar with a registered fallback (Brier tiebreak) avoided endless re-litigation once all four candidates missed the bar by similar small margins | scripts/analyze_receiving_residuals.py (step 27), scripts/test_conditional_sigma.py (step 28, FAIL 6.25pp), scripts/test_conditional_sigma_v2.py (step 29, FAIL 5.74pp), scripts/test_quantile_regression.py (step 30, FAIL 11.28pp raw), scripts/test_coverage_tieaware.py (step 31: tie-aware max|dev| 27=11.99pp / 28=6.25pp / 29=5.74pp / 30=5.66pp, all FAIL; only qreg tie-cells moved >0.5pp), scripts/tiebreak_prob_quality.py (step 32: Brier overall cond-pos=0.22670 vs qreg=0.22809; line>=25.5 cond-pos=0.22450 vs qreg=0.22637 -> cond-pos wins) |
| 2026-06-15 | Models | Receiving calibration: RollingCalibrator(n0=150) adopted for receiving per Part A walk-forward | Reduces summed |bias| (2.03 -> 1.80) with summed MAE improving (62.97 -> 62.35) -- helps at zero cost, mirroring the QB-model adoption rationale | scripts/analyze_receiving_residuals.py Part A output (2022-2025 walk-forward) |
| 2026-06-15 | Models | Production artifacts for the receiving probability layer: models/receiving_yards_ridge.pkl (Ridge pipeline trained on all seasons 2021-2025), models/receiving_sigma_table.parquet (position x prediction-quintile bin medians/stds), models/receiving_z_pool.npy (residual/sigma_pos, mean-centered); z-pool built on all four OOS seasons (2022-2025) by design for sigma-bin sample size, giving skew ~1.10 and median z -0.20, slightly stronger than the 2022-24 exploration figure (-0.18) due to proportionally more RB/TE rows; TE sigma bins enforced non-decreasing via cummax, with the raw quintile-4 dip (29.36->28.00) attributed to TE small-sample noise, consistent with the step-28 smooth-monotonic heteroskedasticity finding. src/models/predict_receiving.py (load_artifacts/sigma_for/prob_over/predict_quantiles) validated by scripts/test_predict_receiving.py (6/6 hard assertions pass), including a regression-guard recalibration: prob_over(pred,pred) reduces algebraically to P(z_pool>0)=0.4187 (an invariant of the artifact), so the test's lower bound was set to 0.41 (from an exploratory 0.42) to match the validated production z-pool -- the right-skew property itself (center < 0.50, p50 < pred) is unchanged and still asserted | Calibrates a regression-guard threshold to the artifact that actually shipped, rather than an earlier exploration-stage pool, without relaxing any of the substantive coverage/skew/monotonicity checks | scripts/build_receiving_prob_artifacts.py output (pool n=9771, z-pool std=0.9982, skew=1.0978, mean~0); scripts/test_predict_receiving.py (6/6 pass) |
| 2026-06-15 | Features | Position sourced canonically from players.parquet (gsis_id -> position) in BOTH the training/matrix path (build_receiving_base) and the prediction path (build_receiving_prediction_features, which inherits it via base), replacing the per-week snap_counts label that flipped on upstream mislabels (e.g. Darren Waller tagged WR in 2025 wk13, TE every other week, which caused a one-off pos_WR/pos_TE divergence in the prediction-features replay test). Snap label retained only as fallback for gsis_ids absent from the crosswalk (0 rows needed it on the 2021-2025 data). 237 (player, week) rows reclassified out of 30113 (0.79%): 82 within {RB,TE,WR} (re-binned), and 155 moved to canonical FB (123) / CB (32) -- players who took WR/TE/RB offensive snaps but are canonically fullbacks/cornerbacks. Of those, 14 survive into the prop pool (targets_l8>=3): Travis Hunter (a genuine two-way CB/WR for JAX in 2025), Bo Melton, Connor Heyward. These stay in the matrix/training as RB-baseline-encoded rows (pos_WR=0/pos_TE=0) but are EXCLUDED from per-position sigma/z-pool fitting (too few to estimate, and a 1-row FB bin yields NaN std); build_receiving_prob_artifacts.py restricts sigma fitting to MODELED_POSITIONS=[RB,TE,WR], and predict_receiving.sigma_for maps any unmodeled position to the RB baseline (SIGMA_FALLBACK_POSITION), consistent with how the point model already encodes them. Artifacts rebuilt in order (train_receiving.py -> build_receiving_prob_artifacts.py): sigma table and z-pool confirmed stable -- pool n 9771->9757 (the 14 FB/CB rows dropped), z-pool std 0.9982->0.9983, skew 1.0978->1.0977, every sigma bin unchanged to the second decimal; test_predict_receiving.py pinned values stayed within band (no update needed). | The per-week snap label is not a reliable position source -- it flips on upstream mislabels and made the same player's position depend on which week's snap row happened to be most recent, breaking training/prediction consistency. players.parquet carries one stable label per gsis_id. The fix touches few rows by design, so the validated probability artifacts should not move materially -- and they didn't, confirming the change is a clean correctness fix rather than a model-behavior change. | build_receiving_base / build_receiving_prediction_features output (237 reclassified, 0 fallback; per-position counts RB 7635->7621, TE 8533->8466, WR 13945->13871, FB 0->123, CB 0->32); build_receiving_prob_artifacts.py output (14 FB/CB excluded from prop pool, n=9757, std=0.9983, skew=1.0977); test_receiving_features.py (6/6), test_receiving_prediction_features.py (Waller divergence gone, pos_WR/pos_TE now hard-asserted, 3/3), test_predict_receiving.py (6/6) |
| 2026-06-15 | Models | Receiving weekly runner: run_week_receiving(season, week, line_overrides, lines, label, force) added to src/models/predict_receiving.py, mirroring the QB run_week. Trains the receiving Ridge pipeline on all rows strictly before (season, week) via preprocess_receiving_matrix (full-history retraining); separately fits a pre-season pipeline and replays the current season's completed weeks 1..week-1 through a single global RollingCalibrator(n0=150) (stateless train-before-each-week replay, one offset across all receivers, same pattern as the QB runner). Target-week rows come from build_receiving_prediction_features (per-team resolved rosters, multi-player). Probability layer is the key departure from QB: per-row conditional sigma via sigma_for(position, calibrated_pred) and per-row quantiles (p10/p25/p50/p75/p90) via predict_quantiles; if lines (dict keyed by gsis_id) is given, attaches line + prob_over per row; unmodeled positions (FB/CB) fall back to RB sigma. Output (sorted by calibrated pred desc): season, week, team, opponent, gsis_id, player, position, pred_receiving_yards, five quantiles, sigma, calibrator_offset, optional line/prob_over, generated_at_utc. Saved to data/predictions/{season}_w{week:02d}_receiving_yards{_label}.parquet under the same immutability rule (FileExistsError unless force=True). Runnable via python -m src.models.predict_receiving --season --week [--label] [--force]. | Reuses the QB runner's validated train/replay/immutable-log skeleton so the two stacks behave identically operationally, while swapping the pooled-residual probability layer for the position+prediction-conditional sigma layer that the 27-35 evidence chain selected for receiving. Per-row sigma is required because receiving residual spread varies ~2x by position and volume (a single pooled pool would mis-size every interval). | Dry-run run_week_receiving(2025, 8): 273 rows, top-20 sensible (Drake London 81.2, Ja'Marr Chase 80.7; McCaffrey RB sigma 23.2 vs WR ~40; right-skew p50<pred throughout); re-run via CLI without --force raised FileExistsError (exit 1), confirming immutable logs |
| 2026-06-15 | Models | Receiving grader: grade_week_receiving(season, week, force) in src/models/evaluate_receiving.py (parallel module, reusing evaluate.py's _quantile_bucket/QUANTILE_BUCKETS/EXPECTED_BUCKET_FREQ and over/under logic), grading the CANONICAL data/predictions/{season}_w{week:02d}_receiving_yards.parquet (not a labeled dry-run). Three-way play classification because the prediction population is a resolved multi-player roster, not a single starter: GRADED (predicted player has a receiving_matrix row that week -- played and recorded, incl. played-uninvolved 0-yard rows via the spine zero-fill, so matrix-row presence is an explicit marker, NOT a non-null yardage check); DID_NOT_PLAY (no matrix row AND no snap_counts row that week -- roster staleness/inactive); NO_STATS (has a snap_counts row but no matrix row -- 0 offensive snaps, non-skill snap position, or unmatched crosswalk -- a genuine data gap). snap presence is resolved by mapping snap_counts.pfr_player_id -> gsis_id via the players crosswalk. Availability accuracy = GRADED/(GRADED+DID_NOT_PLAY) is the roster-resolution quality metric (NO_STATS excluded as a data gap, not a roster miss), analogous to the QB grader's starter accuracy. Reports MAE/bias on all GRADED and on the prop-relevant slice (actual targets_l8>=3, pulled from the matrix since the prediction log doesn't carry it), plus quantile-bucket calibration vs expected 10/15/25/25/15/10. Grades saved to ..._receiving_yards_grades.parquet under the immutability rule; runnable via python -m src.models.evaluate_receiving --season --week [--force]. | The single-starter WRONG_STARTER classification from the QB grader doesn't fit a multi-player roster -- a predicted receiver can fail to appear for two operationally different reasons (didn't play vs. data gap) that must be separated so availability accuracy measures roster-resolution quality without being polluted by upstream stat gaps. Prop-relevant MAE/bias and quantile calibration are tracked on the targets_l8>=3 slice because that is the population props are written on. | grade_week_receiving(2025, 8) on the regenerated canonical file: 273 rows (GRADED=241, DID_NOT_PLAY=28, NO_STATS=4), availability accuracy 241/269=0.896, MAE 14.97 / bias -0.38 all GRADED, MAE 20.46 / bias -0.10 prop-relevant (n=103); quantile buckets one-week-noisy (tails light: below_p10 4.6%, above_p90 6.6%; middle heavy: p50-p75 36.1%) -- the season-replay accumulation is what will make this readable |
| 2026-06-15 | Models | Receiving full-season replay (2025, n=5,968) via scripts/replay_season_receiving.py: availability accuracy 0.895 (stable across segments -- reg 0.895 / playoffs 0.902 -- and positions -- WR 0.882 / TE 0.908 / RB 0.905); prop-slice MAE 22.83 (matches walk-forward ~23), bias +0.09. Calibration verdict: all-GRADED quantile buckets appear over-wide (center-heavy/tails-light: below_p10 3.9%, p50-p75 36.2%) but this is fully attributable to the ~100 played-uninvolved zero-yard rows/week whose sigma was never fit for them; on the prop-relevant slice (targets_l8>=3, n=2,193) -- the only population props are written on -- buckets are textbook (8.9/15.0/26.9/25.5/14.3/9.5 vs 10/15/25/25/15/10). Per-position calibration confirmed strong (WR near-perfect 9.1/14.8/27.2/25.5/13.7/9.6; TE strong 7.8/12.6/27.4/27.5/15.3/9.4; RB good 9.9/18.9/25.0/23.0/14.5/8.7), validating the step-29 conditional-sigma architecture on live graded outcomes -- the check holdout coverage couldn't deliver. Prop-MAE ordering WR 25.7 > TE 20.4 > RB 15.7 reproduces the step-27 heteroskedasticity finding. Decisions: (a) all-GRADED-vs-prop gap documented as expected, NO low-volume sigma-floor added (the product only serves the prop population, where calibration is already correct); (b) Travis Hunter two-way limitation now quantified (6 graded wk, MAE 28.85 vs 25.7 WR-avg, RB-baseline sigma under-covers his WR-like usage, 2x above_p90 in wk5/wk7) -- remains a documented manual-discount flag; usage-based position override deferred as a future feature pending more two-way cases. | The prop-relevant slice is the population the product serves and where sigma was fit, so its textbook calibration is the verdict that matters; the all-GRADED over-width is an explained artifact of out-of-population low-volume rows, not a miscalibration to fix. Per-position calibration is the specific evidence holdout coverage could not produce (too few cells per position-quartile), so reproducing 10/15/25/25/15/10 within each position over thousands of live outcomes is the strongest validation of the conditional-sigma choice. A sigma-floor or two-way override would add complexity for populations the product does not price (low-volume rows) or a single edge case (Hunter), so both are deferred. | scripts/replay_season_receiving.py output (weeks 1-22: total 5,968 rows, GRADED 5,172 / DID_NOT_PLAY 605 / NO_STATS 191; availability 5172/5777=0.895; all-GRADED MAE 15.55 bias -0.57; prop MAE 22.83 bias +0.09; full-season + per-position quantile-bucket tables; Travis Hunter spotlight 6 graded weeks MAE 28.85, RB-baseline sigma) |
| 2026-06-16 | Ingestion | Player-name -> gsis_id resolver: src/ingestion/player_resolver.py (pure matching, no live API). normalize_name lowercases, joins across apostrophes/periods/hyphens (D.K.->dk, De'Von->devon, Amon-Ra->amonra), spaces other punctuation, drops generational suffixes (Jr/Sr/II/III/IV/V), collapses whitespace -- so suffix-drop and punctuation variants collapse to an EXACT match. build_candidate_pool(season, week) returns the team-scoped set of players we would actually predict that week (receiving prediction roster via build_receiving_prediction_features + QB starters via resolve_starters), NEVER the full crosswalk -- a name can only resolve to someone being priced. resolve_player(book_name, team, pool) is team-scoped: (a) unique exact normalized match -> method=exact; (b) else difflib.SequenceMatcher fuzzy with best>=0.82 AND best-second>=0.08 margin -> method=fuzzy; (c) else method=unresolved with top candidates logged. Ambiguous ties (>1 exact, or fuzzy without clear margin) return unresolved -- never picks among tied candidates. resolve_prop_lines(lines_df, season, week) augments rows with gsis_id/method/score and prints counts-by-method + every fuzzy match (eyeball) + every unresolved name. | A wrong silent match (e.g. a nickname or a same-name teammate resolving to the wrong gsis_id) corrupts grading invisibly, which is worse than an unresolved line a human can fix; team-scoping plus exact-first plus a margin-gated fuzzy fallback makes guessing structurally hard, and bounding the candidate set to the week's actual prediction roster means there is nothing to mismatch to outside it. No new dependencies (difflib is stdlib). Ready to drop into the August prop pull. | scripts/test_player_resolver.py: 26 book-style variants vs the real 2025 wk8 pool (size 305) -> 17 exact (incl. suffix-drop "Michael Pittman" and punctuation "DK Metcalf"/"Jamarr Chase"/"Devon Achane"), 3 fuzzy all correct at 0.96-0.97 (JuJu Smith Schuster, Justin Jeferson, Cristian McCaffrey), 6 unresolved -- nickname "Hollywood Brown" stays unresolved (Marquise Brown only 0.414), right-name-wrong-team "Ja'Marr Chase"@BUF unresolved (top 0.48 via team scoping), fakes unresolved; synthetic two-"Mike Williams" tie -> unresolved; hard assertion that no name silently resolves to a wrong gsis_id holds across all cases |
| 2026-06-16 | Models | Receiving prediction-log schema parity + roster-staleness visibility (two metadata-only fixes from the Step-40 rehearsal; no model/artifact change). (1) team_implied_total: run_week_receiving built it in the feature frame but dropped it from the saved output (unlike the QB runner) -- now carried through to the log, bringing receiving to schema parity with QB and letting consumers read the implied total directly. (2) as_of: build_receiving_prediction_features now emits an as_of metadata column ("{season} wk{week:02d}", zero-padded for chronological string sort) recording the game each player's roster row was drawn from -- the receiving analogue of the QB starter as_of -- carried into the runner's log. Both are metadata, not model features: no retraining, no artifact rebuild. Replay-equivalence preserved -- test (a) compares an explicit COMPARE_COLS list (as_of excluded), test (c) now asserts the column set equals receiving_matrix plus exactly the {as_of} metadata column. | The Step-40 rehearsal could only verify implied-total coverage by rebuilding the feature frame because the log didn't carry it; parity removes that workaround and matches the QB log. as_of is what makes the September roster review tractable -- sort by staleness, eyeball the oldest -- since 2026 offseason departures are not detectable from data alone and otherwise sit invisibly on 2025-final rosters. | test_receiving_prediction_features.py (3/3, as_of excluded from feature comparison) and test_receiving_features.py (6/6) still pass; Step-40 rehearsal re-run: team_implied_total populated 0/333 missing (checked directly on result), as_of distribution 2025 wk18=188 / wk19=63 / wk20=40 / wk21=23 / wk22=19 (playoff teams fresher), 0/333 older than 2025 wk18 (expected for a week-1 prediction -- every team played through wk18) |
| 2026-06-17 | Features | RB rushing matrix built (src/features/rushing.py build_rushing_matrix -> data/features/rushing_matrix.parquet, 7606 rows x 49 cols). Base is the canonical-RB snap spine (step-35 position from players.parquet; join-failure 0.05%, 29 snap-labeled RBs reclassified to non-RB canonical and dropped, 952 dressed-no-touch rows zero-filled on counting stats). Rolling uses the SHARED generic roller (add_rolling_features grouped by gsis_id, same machinery as QB/receiving -- now three callers) over 6 stats: carries, rushing_yards, rushing_tds, rushing_first_downs, carry_share, offense_pct -> 18 rolling cols (l3/l8/std) + games_into_season. Excluded from rolling: rushing_epa (NaN-heavy ~12-16%, like racr in receiving) and rushing_fumbles_lost (too sparse/noisy to roll usefully). carry_share is a derived backfield-share metric (player carries / team total RB carries that game; QB scrambles & WR end-arounds excluded; dressed-zero-carry rows confirmed carry_share=0). | No pre-computed rushing-usage share exists in player_stats (only receiving target_share/air_yards_share), so backfield carry_share is derived; EPA and fumbles are excluded from rolling for the same NaN/sparsity reasons that excluded racr in receiving, keeping the rolling set to stable, dense usage/production signals. Reusing the shared roller keeps the three position tables' rolling semantics identical (shift-then-roll, games-not-weeks, season-reset _std). | scripts/test_rushing_features.py 6/6 pass: (a) shift, (b) first-game all-NaN, (c) no cross-player bleed, (d) Derrick Henry 2025 wk9 carries_l3=20.0 = mean of PLAYED weeks 5/6/8 (games-not-weeks, crossing absent wk7), (e) season-to-date _std reset, (f) Mark Ingram dressed-zero-carry counts as 0 in next L3; shared-roller regression check: test_receiving_features.py 6/6 and test_rolling_features.py still pass |
| 2026-06-18 | Models | RB rushing probability layer = quantile regression (7 HistGradientBoostingRegressor loss=quantile, max_depth=3, q in [.05,.10,.25,.50,.75,.90,.95], trained on all prop-slice rows carries_l8>=8 / 2021-2025), productionized at models/rushing_quantile_models.pkl ({q: model}) with src/models/predict_rushing.py (load_artifacts / predict_quantiles sorted-on-output / prob_over piecewise-linear CDF clamped 0.01-0.99). The 45->46 chain: (45) RollingCalibrator(n0=150) ADOPTED -- a free win, summed |bias| 3.15->1.00 (-2.15) at NEGATIVE MAE cost (83.01->82.78); heteroskedasticity present (~1.3x std by pred-quartile and by carries_l8, milder than receiving's ~2x) but a pooled single-distribution FAILS per-quartile coverage (bottom-quartile p10 = -1.84, a negative-yardage floor). (46) cond-sigma (decile, no position axis) vs quantile-reg, both under TIE-AWARE coverage from the outset (receiving step-31 lesson applied upfront, not retrofitted); NEITHER cleared the pre-registered 5pp bar (max|dev|: cond-sigma 8.83pp, quantile-reg 6.29pp); per the registered floor rule quantile-reg ships -- it learns the zero-floor directly, whereas cond-sigma's symmetric z-pool drives negative-yardage lower tails on low-volume backs (bottom-quartile observed p10 collapses to 1.20%). Documented blemish: bottom predicted-value-quartile p50 over-coverage (~6pp) + out-of-sample crossing rate 25.8% on the 2025 test (training-row crossing only 4.24%, magnitude mean 3.3 yds / max 19.75; handled by the mandatory output sort) -- both tracked in live grading. Interface differs from receiving: the rushing layer needs the FULL feature row (quantile models use all FEATURE_COLS), not just a scalar prediction. | Quantile-reg over cond-sigma specifically because rushing's large near-zero mass (low-volume backs, 0-3 yard games) breaks a symmetric-z conditional-sigma at the floor (negative-yardage tails), while quantile models place low quantiles realistically against that mass; with neither architecture clearing 5pp, the pre-registered floor rule (best max|dev|) is the tie-free decider. Calibration adopted on the same <=0.5-MAE-cost rule as receiving. Thin point-model edge over naive (step 44: ~1-2 MAE on prop slice, tied on all rows) means rushing is a hard market -- intervals are honest but wide (residual std ~35). | scripts/build_rushing_prob_artifacts.py (3398 prop-slice training rows, per-quantile in-sample coverage 5/10/25/50/75/90/95 all within 0.15pp, crossing rate 4.24% / magnitude 3.3 yds); scripts/test_predict_rushing.py 5/5 pass (a prob_over decreasing in line, b quantiles strictly increasing post-sort, c prob_over in [0.01,0.99], d workhorse q50 74.56 > low-volume 36.10, e floor: lowest prop-slice q50 row q10=0.53>=0). NOTE: quantile-reg is not bounded >=0 by construction -- an out-of-prop-slice near-zero-volume back extrapolates to a mildly negative q10 (-2.1), which is also physically real (negative rushing games happen); the floor property is asserted on the prop slice the layer serves |
| 2026-06-18 | Models | RB rushing weekly runner: run_week_rushing(season, week, line_overrides, lines, label, force) added to src/models/predict_rushing.py, mirroring run_week_receiving operationally but HYBRID by design -- two distinct estimators surfaced side by side. Headline point: calibrated Ridge (StandardScaler+Ridge on all rows strictly before (season, week)) + global RollingCalibrator(n0=150) replayed over the current season's completed weeks (stateless train-before-each-week) -> pred_rushing_yards. Distribution + prob_over: the STATIC pre-fit quantile models (load_artifacts, built once on 2021-2025), NOT retrained per week -> q05..q95 (sorted) and prob_over (quantile-CDF interpolation). Columns named by source so the two estimates are unambiguous: pred_rushing_yards (calibrated Ridge) vs q50 (quantile model). Target rows from build_rushing_prediction_features (RB roster), carrying team_implied_total + as_of. Output sorted by pred_rushing_yards desc; saved to data/predictions/{season}_w{week:02d}_rushing_yards{_label}.parquet under the immutability rule (FileExistsError unless force). CLI: python -m src.models.predict_rushing --season --week [--label] [--force]. | Hybrid because the calibrated Ridge is the better-de-biased point estimate (step 45 calibration win) while the quantile models own the distribution (step 46 winner); keeping both, explicitly named, avoids forcing one estimator to do both jobs and lets q50-vs-Ridge divergence be a visible sanity signal. Quantile models loaded-not-retrained because they are season-static artifacts -- IN-SEASON STALENESS CAVEAT: the point model and calibrator absorb current-season games each week, but the distribution does not until the artifacts are rebuilt; acceptable at a weekly cadence, revisit on material drift. | Dry-run run_week_rushing(2025, 8, label=dryrun): 74 rows; top-20 sensible (Jonathan Taylor pred 93.88/q50 94.80, James Cook 81.54/86.03, Derrick Henry 75.06/72.58); close-but-distinct estimators confirmed -- largest pred-vs-q50 gaps ~12-15 yds (Josh Jacobs 70.44/55.73, Rico Dowdle 76.55/62.15) where Ridge runs above the conservative quantile median; CLI re-run without --force raised FileExistsError (exit 1), confirming immutable logs |
| 2026-06-18 | Models | RB rushing grader: grade_week_rushing(season, week, force) in src/models/evaluate_rushing.py (new parallel module, reusing _snap_gsis_ids from evaluate_receiving). Grades the CANONICAL data/predictions/{season}_w{week:02d}_rushing_yards.parquet (not the dryrun label). Three-way play classification identical to receiving (GRADED via explicit matrix_row marker since dressed-zero-carry backs sit at 0; DID_NOT_PLAY = no matrix row + no snap row; NO_STATS = snap row but no matrix row), availability accuracy = GRADED/(GRADED+DID_NOT_PLAY). Two rushing adaptations: (1) POINT grading on the calibrated-Ridge headline (pred_rushing_yards - actual) on all GRADED and the prop slice (actual carries_l8>=8) -- the QB/receiving-comparable MAE; (2) QUANTILE-BUCKET calibration graded against the QUANTILE-MODEL distribution columns (q10/q25/q50/q75/q90), not a residual pool -- the structural difference from QB/receiving whose intervals came from pooled/conditional residuals. Bucketing is TIE-AWARE: an actual exactly equal to a quantile boundary splits 0.5/0.5 across the two adjacent buckets (the zero-mass-floor lesson from step 47 / receiving step-31), with boundary-tie count reported. over/under graded if line/prob_over present, skipped gracefully otherwise. Immutable ..._rushing_yards_grades.parquet; CLI python -m src.models.evaluate_rushing --season --week [--force]. | The hybrid log has two estimators, so point and distribution are graded separately and against their own source: MAE on the calibrated-Ridge headline (comparable across all three position models), bucket calibration on the quantile models that actually own the intervals. Tie-aware bucketing is required because rushing's near-zero mass can place a literal point mass on a quantile boundary; the receiving residual-pool graders never needed it (continuous residual thresholds), but the quantile-model boundaries can coincide with integer-yard actuals. | grade_week_rushing(2025, 8) on the regenerated canonical file: 74 rows (GRADED=65, DID_NOT_PLAY=7, NO_STATS=2), availability 65/72=0.903, point MAE 25.09 / bias -4.41 all-GRADED (Ridge over-predicts low-volume backs), MAE 36.24 / bias -0.58 prop-relevant (n=28); quantile buckets tails-heavy this single week (below_q10 20.0%, above_q90 18.5% vs 10% -- opposite of receiving wk8's middle-heavy, hinting slightly narrow intervals, but n=65 and noisy -- season replay is the verdict); 0 boundary ties fired (continuous float quantiles rarely equal integer actuals), tie-aware machinery in place for the zero-mass case |
| 2026-06-18 | Models | RB rushing full-season replay (2025, n=1,515) via scripts/replay_season_rushing.py: availability 0.887 (stable across segments -- reg 0.885 / playoffs 0.914); prop-slice point-MAE 26.66, bias -1.54 (wide but near-unbiased -- rushing is a high-variance market). Calibration verdict: the week-8 tails-heavy hint was small-sample noise -- on the prop slice (carries_l8>=8, n=630) the shipped quantile-reg layer reproduces 10/15/25/25/15/10 within ~2pp (7.5/15.4/27.0/25.7/14.3/10.2), with combined tail mass 17.6% (honest-to-slightly-conservative, NOT too narrow; the >25% "too narrow" flag did not trigger). All-GRADED buckets look low-end-heavy (below_q10 17%) but this is the low-volume-contamination artifact (dressed/low-carry backs at 0), same illusion as receiving -- product only serves the prop slice. Validates the step-46 quantile-reg/floor-rule choice on live graded outcomes; the documented 6.29pp blemish and 25.8% crossing caveat do not surface as aggregate problems at season scale. q50-vs-Ridge gap +4.58 (Ridge higher) confirms right-skew at scale. Operational consequence: rushing's wide intervals (~27 MAE) mean its edge threshold should be set more conservatively than QB/receiving -- only large projection-vs-line gaps clear a useful edge. | The prop-slice tie-aware bucket table over 630 live graded outcomes is the calibration verdict the single-season 5pp tie-aware bar (step 46) could not deliver; reproducing nominal within ~2pp plus a sub-20% tail mass confirms the shipped intervals are trustworthy on the population priced, and the all-GRADED low-end heaviness is an out-of-population artifact (not a defect) consistent with the receiving replay. The wide point MAE is intrinsic to rushing volatility, not a model failure, so it constrains bet-sizing/thresholds rather than invalidating the layer. | scripts/replay_season_rushing.py output (weeks 1-22: total 1,515 rows, GRADED 1,282 / DID_NOT_PLAY 164 / NO_STATS 69; availability 1282/1446=0.887; all-GRADED point MAE 20.96 bias -1.09; prop point MAE 26.66 bias -1.54; full-season + prop tie-aware bucket tables; tail-dispersion read 7.5%/10.2%/17.6% combined; q50-vs-Ridge mean|gap| 6.54 / mean +4.58) |
| 2026-08-11 | Features | DIAGNOSTIC ONLY, NO FIX ADOPTED -- snap_counts position label `HB` silently drops a whole team's backfield from BOTH skill-position spines. Found via the Step-52 wk1 rehearsal, where CIN resolved only 1 back (Kendall Milton, as_of 2025 wk11, 4 snaps / 2 carries) against a league median of 3. Mechanism: `_build_rb_spine` gates candidacy on `snap_counts["position"] == "RB"` (exact match) and receiving's `_build_spine` on `SPINE_POSITIONS = [WR, TE, RB]` (isin) -- neither admits `HB`. CIN's entire 2025 backfield (Chase Brown, Samaje Perine, Tahj Brooks) is labeled HB in all 17 weeks; all three are canonically RB in players.parquet, so the step-35 canonical-position fix never fires -- it resolves the position VALUE but runs after candidacy, which already excluded them. NOT a crosswalk failure (all three map cleanly to gsis_ids). Scope in current data: HB appears in exactly one (season, team) -- CIN 2025, 39 offensive-snap rows, 0 in 2021-2024 -- so this is one team-season, but the defect is structural (an exact-match label gate with no canonical fallback), not a one-off bad row. Impact: 39 back-games absent from rushing_matrix (CIN 2025 has 1 row, not ~40) and from receiving_matrix (Chase Brown has 0 receiving rows for 2025 despite being CIN's lead back), plus the wk1 roster resolving to a 4-snap third-stringer. Fix deferred to a scoped task -- the candidate fix (admit HB at candidacy, let canonical position decide the value) touches both spines and would require rebuilding both matrices and revalidating the probability artifacts. | An exact-match snap-label gate assumes the upstream label vocabulary is closed; it is not, and a single unrecognized synonym removes a team's whole position group silently -- no join-failure counter fires (the crosswalk matched), no canonical-position counter fires (candidacy already dropped the rows), so the only visible symptom was a thin prediction roster three steps downstream. Logged now, unfixed, because the fix changes the training population for two shipped models and is a pre-registered-revalidation job, not a same-session retrofit. | scratchpad diagnostic: CIN 2025 wk12-18 snap-labeled RB rows with offense_snaps>0 = 0, while CIN has 44-48 snap rows/week those weeks; CIN 2025 back labels by week show Chase Brown HB x17, Samaje Perine HB x16, Tahj Brooks HB x7, Kendall Milton RB x1 (wk11, the only spine survivor); players.parquet canonical position = RB for all four; league-wide HB offensive-snap rows by season 2021-2024 = 0, 2025 = 39 (CIN only); 2025 team-games with <2 canonical RBs = 1/554 (CIN wk11); 2025 RB crosswalk failure rate 4/1487 = 0.27% (all ATL/Nathan Carter, unrelated) |
| 2026-08-11 | Models | run_week_rushing's saved log now carries `carries_l8` alongside `team_implied_total` and `as_of`. Log metadata only -- carries_l8 is ALREADY a model FEATURE_COL (it comes from RUSHING_ROLLING_COLS), so this changes the output column set only: no retraining, no artifact rebuild, no feature-frame change. | carries_l8 >= 8 is the prop-slice selector the rushing layer was fitted on and is graded against (steps 46/51); without it in the log, a live prediction file cannot be filtered to the priced population without rejoining rushing_matrix, which defeats the point of an immutable standalone log. | scripts/test_rushing_prediction_features.py 3/3 still pass unchanged (the feature frame was not touched -- only the runner's output selection -- so test (c)'s "column set == rushing_matrix + {as_of}" assertion still holds); Step-52 rehearsal re-run with the column present: carries_l8 missing 0/83, prop slice 33/83 rows |
| 2026-08-11 | Features | HB candidacy defect FIXED in both skill-position spines (the step-53 diagnostic). Candidacy order INVERTED in `_build_rb_spine` (rushing) and `_build_player_game_spine` (receiving): all offense_snaps>0 rows -> crosswalk join -> canonical position from players.parquet -> gate on the CANONICAL value (== "RB" for rushing, isin(SPINE_POSITIONS) for receiving). The per-week snap label now gates only rows with no canonical position available, normalized through a new SNAP_LABEL_SYNONYMS map ({"HB": "RB"}); 0 rows needed that fallback on 2021-2025 data. Receiving's canonical-position block moved out of build_receiving_base into the spine builder (position is canonical on return), so the two spines share one resolution order. New `_audit_snap_position_labels` (shared, in receiving.py, called by both builders) prints the full snap-label vocabulary among offensive-snap rows and FLAGs any label outside KNOWN_SNAP_POSITIONS (23 labels observed 2021-2025) -- the counter whose absence let HB hide. Row deltas: rushing +372 admitted / -29 excluded (net +343, matrix 7606 -> 7978); receiving +427 admitted / -155 excluded (net +272, matrix 30113 -> 30385). SIDE EFFECT BEYOND THE HB FIX, FLAGGED NOT RESOLVED: the canonical gate drops the 155 FB/CB rows that step 24 deliberately KEPT as RB-baseline-encoded training rows (123 FB + 32 CB, 9 players incl. Travis Hunter 7 -> 0 rows) -- FB/CB are not in SPINE_POSITIONS, so gating on canonical position excludes them where the old snap-label gate admitted them and only relabeled them. Offsetting gains are larger and more legitimate: 312 snap-labeled-FB rows whose canonical position is RB, 53 snap-labeled-QB rows whose canonical is TE (Taysom-Hill-type), 39 HB, 15 TE, 6 LB. QB stack confirmed UNAFFECTED and not rebuilt (its spine reads player_stats.position, which has no HB in its vocabulary and never touches snap_counts; qb_matrix unchanged at (2799,57)). | An exact-match snap-label gate applied BEFORE canonical resolution makes the step-35 canonical fix unreachable for exactly the rows that need it most -- a player whose snap label is a synonym is excluded before his canonical position is ever read. Inverting the order makes players.parquet the single authority on who is a candidate and what position he is, and reduces the snap label to a fallback for the crosswalk-miss case. The label audit converts a silent exclusion into a loud one: the next unrecognized synonym FLAGs at build time instead of surfacing three steps downstream as a thin prediction roster. The FB/CB drop is logged as an open decision rather than patched, because restoring them is a population choice for two shipped models (step 24's rationale), not a correctness fix. | Rebuild verification: CIN 2025 rushing rows 1 -> 40, CIN 2025 receiving rows 137 -> 176; Chase Brown 2025 0 -> 17 in BOTH matrices, Samaje Perine 0 -> 15, Tahj Brooks 0 -> 7; receiving per-position RB 7621 -> 7978 / TE 8466 -> 8534 / WR 13871 -> 13873, FB 123 -> 0 / CB 32 -> 0. Artifact stability (rebuilt in order train_receiving -> build_receiving_prob_artifacts -> build_rushing_prob_artifacts): receiving sigma bins moved <= 0.36 in every one of the 15 cells (largest RB q1 14.40 -> 14.76); z-pool n 9757 -> 9784, std 0.9983 -> 0.9972, skew 1.0977 -> 1.0985, mean ~0; recalibration invariant P(z_pool>0) 0.4187 -> 0.4183 (pinned lower bound 0.41 NOT updated, still clear). Rushing: training prop-slice rows 3398 -> 3416, crossing rate 4.24% -> 5.12%, crossing magnitude 3.3 -> 2.51 yds (max single inversion 16.72), in-sample per-quantile coverage all within 0.27pp of nominal (was 0.15pp). All 7 test suites pass with NO pinned value updated: test_rolling_features, test_receiving_features, test_receiving_prediction_features (3/3), test_predict_receiving (6/6), test_rushing_features (6/6), test_rushing_prediction_features (3/3), test_predict_rushing (5/5). 2025 season-replay logs deliberately NOT regenerated. |
| 2026-08-11 | Features | UNION candidacy gate adopted in `_build_player_game_spine` (receiving), replacing step 54's canonical-only gate: a row is admitted if EITHER its canonical position (players.parquet) OR its synonym-normalized snap label is in SPINE_POSITIONS; canonical position still supplies the position VALUE. This keeps the step-54 HB fix surgical -- it is now purely ADDITIVE relative to the pre-step-54 population, proven at build time by a -0-excluded assertion in the verbose delta report (union vs pre-54 gate: +427 admitted, -0 excluded). Step 24's FB/CB inclusion is preserved rather than retired by side effect: the 155 rows the canonical-only gate dropped (123 canonical-FB + 32 canonical-CB, admitted via the snap arm because they take WR/TE/RB offensive snaps) are back, carrying their canonical FB/CB position, RB-baseline-encoded by the point model and excluded from per-position sigma/z-pool fitting -- the step-35 machinery, confirmed firing ("Excluding 14 prop-pool rows with unmodeled position: CB 13, FB 1") and sigma_for(FB|CB, x) == sigma_for(RB, x) at predict time via SIGMA_FALLBACK_POSITION. Travis Hunter is restored to 7 matrix rows, so his documented two-way limitation (step 27: RB-baseline sigma under-covers his WR-like usage, manual-discount flag) remains a tracked, visible caveat instead of being silently deleted. Step-54 gains all retained: 312 canonical-RB fullbacks, 53 QB->TE (Taysom-Hill-type), 39 HB, 15 FB->TE, 6 LB->RB, 2 QB->WR. RUSHING UNTOUCHED and deliberately NOT rebuilt -- its gate is canonical `== "RB"` and is unaffected by the union change; verified functionally by re-running build_rushing_base, which reproduced 7,978 rows exactly (CIN 2025 = 40), matching the on-disk matrix. QB likewise unaffected (player_stats.position source, no HB in vocabulary). | A correctness fix should not silently change a population that was chosen deliberately. Step 54's canonical-only gate conflated two separate questions -- "is this player mislabeled?" (the HB defect) and "should canonically-FB/CB players who take skill-position snaps be modeled?" (step 24's answer: yes, as RB-baseline rows). The union gate separates them: the canonical arm fixes mislabels, the snap arm preserves the deliberate population, and their union is a strict superset of the pre-54 spine, so the HB fix can be adopted without re-litigating step 24. Deleting Travis Hunter would also have destroyed the only live evidence about two-way-player coverage, which step 27 explicitly chose to track rather than special-case. | receiving_matrix 30,113 (pre-54) -> 30,385 (step 54) -> 30,540 (step 55); 30,385 + 155 restored = 30,540 and 30,113 + 427 gained = 30,540 both reconcile. Per-position now WR 13,873 / TE 8,534 / RB 7,978 / FB 123 / CB 32. Restored players: Travis Hunter 7 -> 0 -> 7, Bo Melton 25 -> 0 -> 25, Connor Heyward 70 -> 0 -> 70; Chase Brown (the HB fix) 27 -> 44 -> 44. Artifact stability across all three builds (pre-54 -> s54 -> s55): sigma bins moved <= 0.40 in every one of the 15 cells vs pre-54 (largest RB quintile-1 14.40 -> 14.76 -> 14.80); z-pool n 9,757 -> 9,784 -> 9,784 (FB/CB excluded from fitting, so the +155 rows do not enter the pool), std 0.9983 -> 0.9972 -> 0.9976, skew 1.0977 -> 1.0985 -> 1.0992; recalibration invariant P(z_pool>0) 0.4187 -> 0.4183 -> 0.4184, pinned lower bound 0.41 NOT updated. Test suites all pass with NO pinned value shifted: test_rolling_features, test_receiving_features (6/6), test_receiving_prediction_features (3/3), test_predict_receiving (6/6). |
| 2026-08-11 | Models | Week-1 divergence question CLOSED (diagnostic; no model/architecture change). The wk1 rehearsal's elevated Ridge-vs-q50 prop-slice gap (+7.77 on 2026 wk1) is a WEEK-1 STRUCTURAL EFFECT, not a forward-season anomaly: on 2025 graded outcomes the same prop-slice gap is +12.73 in week 1, +4.54 in weeks 2-18, +0.28 in the playoffs -- a monotonic decay, with the 2026 figure sitting BELOW its 2025 week-1 analogue. The calibrator is NOT the driver: calibrator_offset is exactly 0.0 in week 1 (no completed weeks to replay) but averages only +0.33 over weeks 2-18 (range -0.35 to +2.37), ~4% of the week-1 effect; the dominant component is feature immaturity (every _std column filled from its _l8, thin/empty rolling windows). Estimator comparison on 2025 GRADED actuals: q50 has lower MAE than the calibrated-Ridge headline in EVERY segment, prop slice and all-GRADED, with the margin largest in week 1 (prop slice Ridge 30.85 vs q50 21.46, q50 by 9.39; weeks 2-4 by 2.94; weeks 5-18 by 2.27; playoffs by 3.04; full season 26.66 vs 24.00). Week 1 is also the ONLY segment where Ridge bias is positive (+6.55 prop, +8.57 all-GRADED) -- it over-predicts out of the gate; elsewhere Ridge bias is near zero or slightly negative. IMPORTANT METRIC CAVEAT, recorded so this is not misread as "replace Ridge with q50": MAE is median-optimal and q50 IS a median estimate, so the comparison structurally favors it; Ridge is far closer to unbiased (full-season prop bias -1.54 vs q50 -6.12). Neither estimator dominates -- the hybrid design (step 32) stands, unchanged. | Establishing that the week-1 gap is structural and reproducible converts a suspected forward-season defect into an expected, quantified operating condition, which is what the runbook needed in order to say anything useful about early-season weighting. Sizing the calibrator component separately was necessary to avoid attributing the effect to calibration and 'fixing' the wrong thing. The metric caveat is logged because an MAE-only reading would justify demoting the Ridge headline on evidence that is partly an artifact of comparing a median estimator on a median-optimal loss. Week-1 conclusions are marked PROVISIONAL (n=21 prop slice) rather than adopted as a rule. | Computed from the 22 existing 2025 rushing grades files (GRADED n=1,282; prop slice n=630), no re-grading and no replay regeneration. Per-segment prop-slice n: wk1 21, wk2-4 104, wk5-18 470, playoffs 35. Rehearsal re-run against the current (step-54/55) matrices and artifacts with force: 86 rows (was 83), CIN now resolves 2 backs with Chase Brown appearing 11th at 59.32 pred yds (the HB fix landing in a live prediction), min backs/team 1 -> 2, rows with as_of older than 2025 wk18 now 0/86 (was 1); prop-slice gap +7.77 mean / 7.83 mean|gap| (was +7.80 / 7.80), quantile monotonicity 0/86 violations, team_implied_total 0/86 missing. RUNBOOK.md did not exist at repo root and was CREATED at this step (structure reconstructed from DECISIONS.md and the code, flagged as such in the file header); it now documents the three-market cadence incl. rushing predict/grade/rebuild, the conservative rushing edge threshold (§4), early-season caveats with the week-1 table and estimator guidance (§5.1), and the snap-label audit stop-and-investigate rule (§6). |
| 2026-08-26 | Ingestion | Post-refresh artifact rebuild after the August nflverse re-ingestion (raw pulled 2026-08-25; all three matrices had already been rebuilt before this step). WHAT ACTUALLY MOVED: the entire receiving change is driven by players.parquet CANONICAL RELABELING, not by new stats or new games -- snap_counts' offensive-snap composition is byte-identical to step 55 (52,672 rows, same 23-label vocabulary, audit clean), 2025 receiving_yards unchanged on all 5,172 graded rows, 2025 rushing_yards unchanged on all 1,282 graded rows, 2025 team_implied_total unchanged on all 570 graded team-weeks. receiving_matrix 30,540 -> 30,574 decomposes EXACTLY into two relabeled players: Bo Melton CB -> WR canonical (+13 previously-excluded CB-snap rows admitted, plus his 25 existing rows relabeled CB -> WR) and Jack Westover FB -> TE canonical (+21 previously-excluded FB-snap rows admitted, plus 2 existing rows relabeled FB -> TE). That reconciles every bucket: WR +38, TE +23, FB -2, CB -25, RB +0, net +34, all in 2025. Rushing spine counters are byte-identical to step 54 (7,982 candidates, 4 unmatched, +372/-29 inversion, 7,978 rows) and QB is untouched. QB NOT REBUILT: passing_yards is unchanged on all 563 graded 2025 rows when keyed correctly on actual_qb_id (an initial 5-row 'change' was a join bug on my side -- keying the actual starter's yards to the PREDICTED starter's id -- not data drift). TRAVIS HUNTER IS STILL CANONICALLY CB: the refresh did NOT resolve the step-27 two-way limitation upstream. He remains 7 rows (all 2025, position CB), encodes as pos_WR=0/pos_TE=0 (RB baseline) after preprocessing, and sigma_for('CB', x) still returns the RB curve via SIGMA_FALLBACK_POSITION (23.10 at pred=40 vs WR 32.06) -- so his intervals remain too tight for WR-like usage and the documented manual-discount flag stays live. | A refresh that changes row counts is not self-evidently a data-quality event: decomposing the delta to two specific canonical relabels, while proving the stat layer and snap layer did not move, establishes that nothing about the modeled population's measurements changed -- only who is admitted and under which position label. That is the cheapest possible confirmation that the artifacts can be rebuilt without re-validating the underlying architecture. Rebuilding QB on a join bug of my own making would have burned a validated June artifact for no reason, so the drift check was re-run on the correct key before deciding to skip. | Artifact stability (step 55 -> post-refresh). Receiving sigma, all 15 cells: RB 14.803/18.623/19.845/20.836/23.100 -> 14.77/18.65/19.88/20.81/23.10; TE 22.979/23.250/29.417/29.417/32.232 -> 22.78/23.40/29.44/29.44/32.23; WR 24.715/30.810/33.523/37.878/42.392 -> 24.69/30.83/33.33/37.98/42.40 (max move 0.20, TE quintile-1). z-pool n 9,784 -> 9,791, std 0.9976 -> 0.9977, skew 1.0992 -> 1.0998, mean ~0; recalibration invariant P(z_pool>0) 0.4184 -> 0.4182, pinned lower bound 0.41 NOT updated. Unmodeled-position exclusions from sigma fitting 14 -> 7 rows (CB 6, FB 1), consistent with Melton leaving the CB bucket. Rushing: training prop-slice rows 3,416 (unchanged), crossing rate 5.12% -> 5.80%, magnitude 2.51 -> 2.66 yds, max inversion 16.72 -> 19.07, in-sample per-quantile coverage all within 0.18pp of nominal (was 0.27pp). CAVEAT ON THAT MOVEMENT: it is deterministic (random_state fixed; reproduced identically on a second run) at unchanged row count and unchanged spine membership, and 2025 stats/schedule values are verified unchanged -- so it originates in 2021-2024 feature values (historical stat or weather backfill in the refreshed pull), which could NOT be isolated because no pre-refresh baseline for those seasons survives (/data/ is gitignored, the matrices were already rebuilt, and the session scratchpad had been cleared). All 7 test suites pass with NO pinned value shifted: test_rolling_features, test_receiving_features, test_receiving_prediction_features, test_predict_receiving, test_rushing_features, test_rushing_prediction_features, test_predict_rushing. |
| 2026-08-26 | Ingestion | Player-prop parse + consensus + EVENT-SCOPED resolution, built and validated entirely against the saved step-58 payload (no API calls). (1) `parse_event_props(raw_json)` in odds.py flattens a per-event payload to long form, one row per (bookmaker, market, player, side): event_id, home_team/away_team mapped through TEAM_NAME_MAP, bookmaker, market, book_name (from the outcome's `description`), side (from `name`), line (from `point`), price, last_update (market-level; the bookmaker-level field is null on this endpoint). Field roles are INVERTED vs the game-lines endpoint -- there `name` carries the team or Over/Under; here `name` is Over/Under and `description` is the player -- so the two parsers cannot share outcome-handling code. Degrades gracefully to an empty correctly-columned frame on a missing `bookmakers` key, empty bookmakers, null markets, or empty outcomes, and to None-valued fields on a partial outcome. (2) `consensus_prop_lines(props_df)` collapses to one row per (market, book_name): median line across books, n_books, median over_price and under_price kept SEPARATE (the two sides are independently priced and not interchangeable), plus event_id, both teams, and max last_update -- mirroring consensus_game_lines. (3) `resolve_prop_lines_event(consensus_df, season, week)` + `resolve_player_event` in player_resolver.py: NEW functions; `resolve_player`, `resolve_prop_lines`, and `build_candidate_pool` are UNMODIFIED. NO-TEAM-ATTRIBUTION FINDING (step 58): the per-event prop payload carries no per-player team and no player id -- team is known only at event level -- so team-scoping, the guard that made the step-28 right-name-wrong-team case fail closed, cannot be applied directly. AMBIGUITY RULE ADOPTED: rather than widening resolve_player's scope to both teams (which would halve that protection silently), the event resolver calls the unchanged resolve_player ONCE PER TEAM and arbitrates -- exactly one team resolving wins and records `resolved_team`; BOTH teams resolving returns the new method `ambiguous_both_teams` with gsis_id None and both candidates logged; neither resolving returns `unresolved` with candidates merged from both teams, team-prefixed. A name that legitimately matches a player on each side of one game is precisely the case that must not be guessed, so it gets its own method rather than being folded into unresolved. | Keeping the team-scoped matcher intact and arbitrating above it preserves the step-28 property that guessing is structurally hard, while adapting to a payload that cannot supply the team. Splitting `ambiguous_both_teams` out of `unresolved` matters operationally: unresolved means "we could not find this player" (usually fixable by refreshing the roster), while ambiguous means "we found two equally good answers and refuse to choose" (needs a human decision), and collapsing them would hide which. Over/Under prices are medianed separately because a prop with -115/-109 is not the same product as -112/-112 and averaging the two sides would erase the vig asymmetry the edge calculation depends on. | Chain run on props_raw_8c94552d022acec4a0458d70c19d3da9_2026-08-26.json (NE @ SEA, 2026 wk1): parse -> 76 rows x 10 cols (3 books x 3 markets); all 5 degradation cases return correctly-columned frames. consensus -> 14 rows, n_books 3 for 12 pairs and 1 for 2 pairs (DeMario Douglas, Hunter Henry -- FanDuel-only), median lines verified by hand (Drake Maye pass 228.5/229.5/225.5 -> 228.5; over_price -112/-114/-117 -> -114, under_price -112/-114/-114 -> -114). Resolution vs the 2026 wk1 pool: 11 exact, 0 fuzzy, 0 ambiguous_both_teams, 3 unresolved. NO name required the fuzzy path -- every resolution was an exact normalized match. Punctuation cases collapsed correctly: 'A.J. Brown' -> 'aj brown' and 'AJ Barner' -> 'aj barner' normalize DISTINCTLY (asserted -- they must not collide), 'Jaxon Smith-Njigba' -> 'jaxon smithnjigba' and 'DeMario Douglas' -> 'demario douglas' both matched exactly. THE 3 UNRESOLVED ARE ROSTER STALENESS, NOT MATCHING FAILURES -- verified against players.parquet `latest_team`: A.J. Brown (last matrix game 2025 wk19 PHI) is latest_team NE, Romeo Doubs (2025 wk19 GB) is latest_team NE, Jadarian Price is a 2026 rookie (gsis_id 00-0041512, last_season 2026, latest_team SEA, zero matrix rows). The candidate pool is built from 2025-final rosters, so these players are correctly absent from it and the resolver correctly refused to guess (best scores 0.71 / 0.54 / 0.44, all well under the 0.82 fuzzy threshold). NOTE FOR A FUTURE STEP, NOT ACTED ON HERE: players.parquet already carries a `latest_team` column holding the 2026 truth, which is a concrete lead for the roster-staleness problem the `as_of` column was built to surface. |
| 2026-08-26 | Features | Both step-60 roster-resolution defects FIXED, PREDICTION-TIME ONLY. Shared helpers live in receiving.py and are imported by rushing.py (same pattern as the spine helpers). DEFECT B (roster window): `_build_window_roster` replaces "each team's players from its most recent prior game" with "any player with an offensive snap for that team across its last ROSTER_WINDOW_GAMES=4 prior team-games" (named module constant, cross-season allowed). `as_of` is now the PLAYER'S OWN most recent game inside that window rather than the team's, so it still reads per-player as a staleness signal instead of collapsing to one value per team. DEFECT A (team authority): `_apply_latest_team` reassigns each rostered player to players.parquet `latest_team`, in three ordered filters -- (1) status must be in ALLOWED_ROSTER_STATUS={ACT,PUP} or the player is DROPPED, (2) team := latest_team, (3) latest_team must be one of the 32 teams on this week's slate or the player is dropped -- plus a trade collapse (one row per gsis_id, freshest as_of wins). CUT/RES/NWT/RSN are excluded because the step-60 diagnostic showed they retain a latest_team 100% of the time (CUT 59/59, RES 96/96), so latest_team alone cannot distinguish a rostered player from a released one and status is the only discriminator; DEV (practice squad) is likewise excluded by falling outside the allow-set. SCOPE GUARD: `_latest_team_is_authoritative(players, season)` returns True only when `players.last_season.max() == season`, so the override is structurally impossible to apply to any season the crosswalk does not describe -- a 2025 replay cannot be relabelled with 2026 teams. It is self-adjusting (2027 data makes 2027 authoritative). Both builders expose roster_window / apply_latest_team / verbose; passing roster_window=1, apply_latest_team=False reproduces the pre-step-61 board exactly. Historical matrices, build_*_base, and the training path are UNTOUCHED. QB DELIBERATELY NOT CHANGED: resolve_starters returns {team: gsis_id} -- exactly one starter per team, consumed downstream as a dict -- so widening its window would break that contract; QB already has starters_override.csv as its manual lever, and its stale rate was the lowest of the three (3/32). | A one-game roster window and a stale team label are independent defects with independent fixes, so they are staged and measured separately rather than shipped as one change. Gating the override on a computed authority check rather than a hardcoded year or a caller-supplied flag makes the prediction-time-only scope a property of the code instead of a convention someone must remember: there is no argument a caller can pass that relabels 2021 Packers games. Dropping status-failing players outright (rather than leaving them on their old team) is the honest reading -- a released or reserve player is not playing this week at all, so keeping him on the board would manufacture a prediction for someone who cannot record a stat. | Staged board composition, 2026 wk1. RECEIVING: (a) baseline N=1/no override 339 rows / 120 prop-slice / 81 stale; (b) window N=4 441 / 144 / 101; (c) window+override 418 / 141 / 0 stale. RUSHING: (a) 86 / 34 / 23; (b) 115 / 44 / 27; (c) 112 / 44 / 0 stale. Stage (c) eliminates 101 receiving and 27 rushing stale rows -> ZERO on both boards. Stage (b) recovers 36 of the 52 step-60 prop-relevant invisible receivers (incl. Brock Bowers, Breece Hall, De'Von Achane, George Kittle, Nico Collins, T.J. Hockenson, Rashee Rice, Jaylen Waddle, Marvin Harrison Jr.) and 11 of 16 rushing (incl. Breece Hall, Achane, Javonte Williams, Quinshon Judkins, Zach Charbonnet, Isiah Pacheco). The 16 receiving / 5 rushing NOT recovered are players whose last game predates their team's last game by more than 4 team-games -- Malik Nabers (2025 wk4 vs NYG wk18), James Conner (wk3 vs ARI wk18), Garrett Wilson (wk10), Sam LaPorta (wk10), Alvin Kamara (wk12) -- i.e. season-ending injuries, out of reach of N=4 by construction. Dropped between (b) and (c): 23 receiving (RES 12, DEV 7, CUT 4 -- incl. JuJu Smith-Schuster [CUT], Allen Lazard [CUT], Gabe Davis [RES], Ricky Pearsall [RES]) and 3 rushing (DEV 2, RES 1); 0 players ADDED by the override, confirming it only reassigns and never invents. Rookies confirmed EMPIRICALLY (not assumed) to stay out: 0 players with zero matrix history and 0 players with rookie_season>=2026 on either final board, and preprocess filtered 0 rows (418/418, 112/112) because the window roster is derived from the historical spine and cannot admit a player who has never taken a snap. REPLAY EQUIVALENCE HOLDS AND IMPROVED: test_receiving_prediction_features wk5 common 288 rows all features match (only_in_actual 19), wk14 common 288 match (only_in_actual 18, down from 35 pre-change) -- the wider window recovers players who genuinely played, so coverage improved while exactness held; test_rushing_prediction_features wk5 common 71 match, wk14 common 78 match (only_in_actual 5 -> 2). Scope violation check: _latest_team_is_authoritative is False for 2021/2024/2025 and True only for 2026, and a direct audit of the 2025 wk5 board against receiving_matrix found 0/288 team mismatches -- no 2026 team leaked into a historical replay. All 7 suites pass, no pinned value shifted. 2026 wk1 rehearsals re-run (labelled): rushing 112 rows with Kenneth Walker III now on KC (5th, 70.9), Travis Etienne on NO (13th, 57.7), Rico Dowdle on PIT, Tyler Allgeier on ARI, plus De'Von Achane (6th) and Breece Hall (12th) newly present; receiving 418 rows with Bowers/Hall/Achane/Kittle/Collins all present and correctly teamed, Nabers/Wilson/Kamara/LaPorta/Conner still absent as expected. |
| 2026-08-27 | Models | FIRST COMPLETE END-TO-END CHAIN: real sportsbook prop lines -> parse -> consensus -> event-scoped resolution -> priced predictions with prob_over, across all three markets. Run offline against the saved step-58 payload (NE @ SEA, 2026 wk1) plus the June game-lines snapshot for line_overrides; no API calls, everything written under label="props_test" so no canonical log was touched. RESOLUTION IMPROVEMENT ATTRIBUTABLE TO STEP 61: 13 exact / 0 fuzzy / 0 ambiguous / 1 unresolved, versus step 59's 11 / 0 / 0 / 3 on the SAME payload -- the only thing that changed between the two runs is the step-61 roster fix. A.J. Brown resolved exact to 00-0035676 on NE and Romeo Doubs to 00-0037816 on NE (both were 2026 offseason moves invisible to the 2025-final rosters, now visible via the latest_team override); Jadarian Price remains correctly unresolved (2026 rookie, gsis_id 00-0041512, zero matrix history, best fuzzy score 0.435 well under the 0.82 threshold) -- confirming the prediction that two of three would resolve and the rookie would not. Still zero fuzzy matches: every resolution was an exact normalized match. PRICING: 11 of 14 consensus pairs carry a prob_over -- receiving 8/8 resolved rows priced, QB 2/2, rushing 1/3. | The chain now spans every layer that was built separately over steps 17-61, so this is the first evidence that the pieces compose: book payload, name resolution, roster resolution, feature build, three different probability architectures, and market comparison all agree on the same 14 props. Running it on a saved payload rather than a live pull keeps the test reproducible and costs no quota. Display-only market comparison is deliberate: the runbook defers the edge threshold to weeks 5-6 of live calibration, so no edge flagging or bet recommendation appears anywhere in this output. | RESOLVED BUT NOT PRICED (2, both structural, not defects): Drake Maye (line 23.5) and Sam Darnold (line 5.5) have player_rush_yds props, but the rushing board is canonical-RB only by construction (build_rushing_base gates on canonical position == RB), so QB rushing props cannot be priced by the current stack. Books post them; we have no model for them. This is a coverage gap to decide on, not a bug. DIRECTIONAL SANITY: all 11 priced rows are consistent once each market's own geometry is applied. The naive rule (pred > line => prob > 0.5) initially flagged AJ Barner (pred 28.8, line 25.5, margin +3.3, prob_over 0.474), but that rule is WRONG for the receiving layer: prob_over crosses 0.50 at line = pred + median(z)*sigma, and the z-pool median is -0.2039, so the crossover sits 0.204*sigma BELOW the prediction and a positive margin alone is insufficient. Verified per row -- crossover vs line vs prob all consistent for all 8 receiving rows (Barner crossover 23.91 < line 25.5 -> prob < 0.5 correctly; A.J. Brown crossover 61.04 > line 60.5 -> prob 0.506 correctly, only just). The QB pool is near-symmetric (median -0.43, P(r>0)=0.4966) so the naive rule does hold there, and both QB rows passed. Rushing's prob_over comes from the quantile models, not from pred_rushing_yards, so it is checked against q50: George Holani pred 13.8 / q50 15.7 vs line 21.5 -> 0.239, consistent. No extreme (<=0.02 or >=0.98) probabilities. MARKET COMPARISON (display only, de-vigged by normalizing the two American-odds implied probabilities to sum to 1): book vig ~6.3-6.5% on every pair; because prices are near-symmetric (mostly -114/-114) the de-vigged market prob_over is ~0.500 throughout, so our deviations read directly -- largest gaps Jaxon Smith-Njigba -0.156 (we are under, pred 75.5 vs line 84.5), Rashid Shaheed -0.123, DeMario Douglas -0.135, Cooper Kupp +0.074; QB Sam Darnold +0.056, Drake Maye -0.014. NO edge flagging, NO bet recommendation -- pure logging mode per RUNBOOK section 4. Summary: 14 consensus pairs -> 13 resolved -> 11 priced -> 2 resolved-but-unpriced (QB rushing) -> 1 unresolved (rookie). Outputs: data/predictions/2026_w01_{qb_pass_yards,receiving_yards,rushing_yards}_props_test.parquet. |
| 2026-08-27 | Models | QB RUSHING: DECISION NOT TO BUILD before the 2026 season. Read-only population/distribution diagnostic; no model, no matrix, no artifacts. POPULATION: 2,799 starting-QB games 2021-2025 (qb_matrix starter spine joined to player_stats on (season, week, team, player_id)), 100% join. Heavy near-zero mass -- 8.7% zero carries, 19.9% <=0 yards, 36.6% <=5 yards, 50.0% <=10 yards; carries mean 3.87 / median 3, rushing_yards mean 17.74 / median 10 / std 21.18 (p10=0, p25=2, p75=27, p90=47, p99=89). BIMODALITY HYPOTHESIS REJECTED -- this is the finding that killed the two-model design the diagnostic was run to evaluate. Per-QB-season yards-per-game (>=8 starts, n=159) is CONTINUOUS and right-skewed: single mode at 5-10 ypg with monotonic decline, bucket counts 28/35/23/22/14/10/12/5/9 across [0,5)...[50,100), p10=3.1, p50=13.1, p90=37.7. The extremes are dramatic (Justin Fields 2022 76.2 ypg; Tom Brady 2022 -0.06) but the middle is densely populated and NO natural gap separates 'pocket' from 'mobile' -- any cut is a convenience, not a boundary the data supplies. PREDICTABILITY IS MOSTLY BETWEEN-QB, NOT WITHIN-QB: overall corr(L8 rushing yards, next game) = 0.581, but split at L8=10 it is 0.478 mobile (n=1,641) vs 0.198 pocket (n=1,038); at L8=15, 0.439 / 0.282; at L8=20, 0.401 / 0.355. The headline correlation is largely 'mobile QBs run more than pocket QBs' -- a level effect a model does not need to be built to discover. NAIVE-L8 BAR: MAE 12.49 on all starter games (bias +0.11, near-unbiased), 15.91 mobile, 7.08 pocket, against constant-mean MAEs of 16.12 / 18.11 / 7.69. Pocket-QB history buys only 0.61 MAE over predicting a constant, and pocket games are 39% of the population -- effectively unmodellable. MISSING FEATURE IS THE BINDING CONSTRAINT: player_stats carries ONE undifferentiated `carries` count with no designed-run vs scramble split (0 columns match scramble/designed patterns; carries/rushing_yards/rushing_tds/rushing_first_downs/rushing_fumbles_lost are 0.00% null, rushing_epa 12.21% null). nflverse play-by-play exposes qb_scramble, but pbp is NOT ingested in this project. That split is what would separate a designed-run high-floor rusher from a scramble-driven variance rusher, and we cannot compute it. MARKET CONTEXT: books post ~2 QB rushing lines per game (~32 per slate). Well-set lines exist -- Drake Maye's 23.5 sits at his 2025 median of 25.0, splitting outcomes 47.6/52.4 -- but low lines sit on a near-degenerate distribution: for the Darnold-like population (2025 games, L8 in 0-12, n=219) median is 5, P(over 5.5) = 0.457, quartiles 0/5/10, and half the mass sits in 0-5 where a single scramble flips the result. REJECTED SUB-HYPOTHESIS, recorded so it is not re-proposed: the 19.9% <=0-yard mass is NOT explained by kneel-down contamination -- games with <=0 rushing yards show win% 53.2% vs 49.4% for positive-yardage games (mean team margin -0.29 vs +0.17), and corr(rushing_yards, team margin) = 0.032. | Expected gain is thin against a bar that is already near-unbiased: the RB precedent (step 44) is 1-2 MAE over naive, and here naive-L8 is 12.49 with +0.11 bias, so a model would be fighting for a small absolute improvement on half a market. The continuous distribution removes the two-model design that would have justified the complexity, and the low-mobility half -- 39% of games -- is barely separable from a constant. Most decisively, the limiting factor is a MISSING FEATURE rather than a modelling choice, so building now would lock in a weak ceiling; the honest gate is whether to ingest play-by-play, which is a new ingestion layer with its own validation burden (and would also unlock drive context and game script well beyond this one market). Deferring is consistent with how other insufficient-evidence items have been handled -- the DEFENSE_COLS A/B (row 19, eligible for ONE re-test after the 2026 season with criteria re-registered beforehand) and the usage-based two-way position override (rows 27/57, deferred pending more cases). | Deferred to the offseason as a PRE-REGISTERED experiment against fresh 2026 data: acceptance criteria to be registered BEFORE results are seen, per standing discipline. The pbp-ingestion question is the gate and should be decided on its own merits, not as a subordinate step of this market. Diagnostic evidence: population/percentile tables, the 159-QB-season ypg histogram, corr splits at L8 cuts 10/15/20, naive-vs-constant MAE table, null-rate table, and 2025 per-QB actuals for Maye/Darnold/Hurts/Jackson/Allen/Goff/Tagovailoa/Burrow/Mahomes/Murray. NOTE: two items named in the framing of this step -- 'censored-sigma rushing' and 'collinear-feature pruning' -- have NO match in DECISIONS.md and are not cited above; if they are real deferred experiments they are tracked outside this log. |
| 2026-08-27 | Models | QB starter resolution: default rule changed to the MODAL starter across a team's last STARTER_WINDOW_GAMES=4 prior games, ties broken by most recent start (src/models/predict.py resolve_starters). The {team: gsis_id} return contract is preserved exactly (dict, one gsis_id per team, 32 entries, verified); starters_override.csv is untouched and still wins; as_of is now the chosen QB's most recent start INSIDE the window, and a new `window_starts` summary column reports how contested the window was (e.g. 2/4). STARTER_WINDOW_GAMES=1 reproduces the pre-step-67 single-game rule exactly, so reverting is a one-constant change. THE CHANGE DOES NOT ACHIEVE ITS STATED GOAL AND REGRESSES MID-SEASON TRACKING -- recorded here in full rather than quietly shipped. (i) 2026 wk1 before/after: only 2 of 32 teams changed -- DEN Jarrett Stidham -> Bo Nix (3/4, a genuine fix: Nix started wk15-wk20 and Stidham started only the wk21 conference game) and IND Riley Leonard -> Philip Rivers (3/4, a REGRESSION: Rivers has last_season=2025 in players.parquet and is not in the league for 2026). Agreement with the 2025 primary starter moved 24/32 -> 25/32. The step-66 premise was WRONG: these are not week-18 rest starts but multi-game late-season handovers -- KC ran Mahomes wk13-15 then Minshew wk16 then Oladokun wk17-18 (window 2/4 to Oladokun on recency), MIA ran Tua wk13-15 then Ewers wk16-18 (3/4 Ewers), WAS ran Mariota wk13-16 then Josh Johnson wk17-18, ATL ran Kirk Cousins in all of its last 6 games. A modal window cannot distinguish a sustained handover from a rest start because they are the same shape in the data, so 6 of the 8 flagged teams still resolve to the closing QB -- correctly, by the rule's own logic. (ii) Mid-season change tracking, 3 real cases found programmatically (>=4 consecutive starts before and after the switch) out of 27 candidates in 2021-2025: 2021 HOU wk9 Davis Mills -> Tyrod Taylor, 2021 NO wk9 Jameis Winston -> Trevor Siemian, 2022 ATL wk15 Marcus Mariota -> Desmond Ridder. The new rule LAGS THE OLD BY ONE GAME IN EVERY CASE (games-until-tracked OLD 2/1/1 vs NEW 3/2/2) and is less accurate from the switch onward (OLD 50%/50%/75% vs NEW 33%/33%/50%). (iii) No retroactive breakage: test_prediction_features, test_predict, test_calibrate, test_rolling_features all pass unchanged. | The modal window is strictly worse on the criterion it was supposed to be neutral on (mid-season responsiveness) and near-neutral on the criterion it was supposed to fix (week-1 correctness), because the diagnosis behind the fix was wrong. The step-66 dry run described the 8 mismatched teams as week-18 rest starts and benchmarked against 'most starts in 2025'; the last-6-games evidence shows sustained multi-game replacements, for which 'the QB with the most starts across the whole season' is not obviously the right week-1 answer -- MIA is a clear example, since Tua's latest_team is now ATL, making Ewers plausibly correct. Surfacing an out-of-league QB (Rivers) is the concrete cost of widening the window without an activity filter. Per standing discipline the rule was NOT retuned after seeing these results; a different window, a playoff-game exclusion, or a status/last_season filter would each be a separate pre-registered experiment. | Implemented as specified and left in place pending a decision to revert (STARTER_WINDOW_GAMES=1) or to re-scope. Evidence: before/after table for all 32 teams; per-team last-6-game start sequences for KC/IND/MIA/LV/NYJ/WAS/ATL/DEN; 27 detected mid-season changes with 3 traced game-by-game; players.parquet status/last_season checks (Philip Rivers ACT but last_season=2025, Mahomes/Tua/Ewers/Nix all last_season=2026). RUNBOOK §5.2a rewritten to separate the two QB failure modes and to state that this fix addresses neither the dominant one nor removes the need to eyeball all 32 rows weekly. **SUPERSEDED 2026-08-27 (same day): REVERTED to STARTER_WINDOW_GAMES = 1**, restoring the pre-step-67 most-recent-prior-start behaviour exactly -- the 2026 wk1 starter table now reproduces the step-66 output 32/32 (DEN back to Jarrett Stidham, IND back to Riley Leonard), and test_prediction_features / test_predict / test_calibrate / test_rolling_features all pass. The modal machinery is retained but degenerates to the single-game rule at N=1, and `window_starts` is KEPT in the summary as review signal (at N=1 it always reads 1/1, which is itself the reminder that the default rests on one game). This row is deliberately NOT deleted: the negative result is the evidence base for the revert, and re-proposing a wider window should start from it. REVISED READING OF THE 8-TEAM MISMATCH: it is not a resolution-rule defect at all. Who starts in September 2026 is not derivable from 2025 data by ANY rule -- the information (offseason signings, trades, camp battles, recoveries) is simply not in our inputs, so no window length, tie-break, or activity filter can close it. It is an offseason-information gap, and the correct handling is starters_override.csv, which for week 1 is the expected steady state rather than an exception. |
| 2026-08-27 | Features | Roster manual-add mechanism: data/predictions/roster_override.csv (columns season, week, market, team, player_name; market in {receiving, rushing}), read by _apply_roster_override in src/features/receiving.py and called by BOTH build_receiving_prediction_features and build_rushing_prediction_features after the latest_team override. This supplies the mechanism RUNBOOK §5.2b previously assumed but did not have -- it told the operator to 'add them manually' with no way to do so. Name resolution mirrors starters_override.csv (exact players.parquet display_name match; unknown and ambiguous names raise ValueError listing candidates) with two deliberate differences: (1) it does NOT pre-filter by position, so a canonically-CB two-way player such as Travis Hunter stays addressable, and (2) it adds a HISTORY GUARD -- a resolved player with no rows in the relevant spine strictly before (season, week) raises a ValueError naming his position/rookie_season/last_season, rather than injecting a row whose rolling features are all NaN and which preprocess_* would silently drop. A malformed `market` value or a missing required column also raises. Injected players carry their real rolling history and their own as_of (most recent historical game); the override wins on team assignment, replacing any existing row for that gsis_id. | The step-66 dry run hit the exact scenario §5.2b predicts -- Malik Nabers returned from the books with a posted 60.0 receiving line while being invisible to the 4-game roster window -- and there was no way to act on it, so the guidance was unactionable. The history guard is the important design choice: the failure mode being prevented is not a crash but a silent one, where a rookie or long-absent player is added, produces all-NaN features, and disappears in preprocessing without the operator ever learning the add did nothing. Not filtering by position keeps the two-way case reachable, and the history guard is strictly stronger than a position filter would have been. | Happy path: with `2026,1,receiving,NYG,Malik Nabers`, the 2026 wk1 receiving board goes 411 -> 412 rows; Nabers appears on NYG as WR with as_of 2025 wk04 and REAL features (targets_l3 7.667, targets_l8 9.875, receiving_yards_l3 66.67, receiving_yards_l8 82.0, target_share_l8 0.3154, offense_pct_l8 0.82, team_implied_total 23.0), zero NaN among them, and he survives preprocess_receiving_matrix. Without the override he is absent (411 rows). Error paths all verified: unknown name 'Malik Naybers' raises with the no-match message; 'Jadarian Price' (2026 rookie, SEA, rushing) resolves but raises the no-history error naming position=RB / rookie_season=2026 / last_season=2026; market='passing' raises listing the valid markets. Scoping verified: the wk1 receiving row does not appear on the 2026 wk2 receiving board or the 2026 wk1 rushing board. Regression: test_receiving_prediction_features, test_rushing_prediction_features, test_receiving_features, test_rushing_features, test_predict_receiving, test_predict_rushing all pass unchanged. |
| 2026-08-27 | Models | Two guards on starters_override.csv, plus the populated 2026 wk1 override file. (A) SKIP SENTINEL: player_name=SKIP (case-insensitive, whitespace-tolerant; SKIP_SENTINEL in src/models/predict.py) drops a team from the board -- resolve_starters omits it from the returned {team: gsis_id} dict but emits a summary row with source='skip', null qb_id/qb_name and window_starts='skip', so a short board is VISIBLE rather than silently missing. The team's opponent is unaffected (verified: SKIP on ATL leaves PIT resolving normally). (B) NO-HISTORY GUARD, mirroring the step-67B roster_override guard: a named override QB with zero qb_matrix rows strictly before (season, week) now raises a ValueError naming rookie_season / last_season / latest_team, stating that every rolling feature would be NaN, and pointing at SKIP as the intended handling. Previously such a name resolved fine and was then dropped silently in preprocessing -- the override appeared to work and did nothing. (C) data/predictions/starters_override.csv WRITTEN for 2026 wk1 with 8 rows: ATL=SKIP, DEN=Bo Nix, IND=Daniel Jones, KC=Patrick Mahomes, LV=SKIP, MIA=Malik Willis, NYJ=Geno Smith, WAS=Jayden Daniels. | The 8-team week-1 mismatch is an offseason-information gap (see the superseded step-67 row A), so the override file is the only instrument -- but an override file needs to be able to express 'I do not know', or it forces a guess. A two-way QB competition turns on contract commitments, an organisation's read on a declining veteran, and willingness to commit to a young starter; none of that is in passing data, so choosing the QB with better 2025 numbers is a coin flip presented as analysis, and it would produce a logged prediction we then grade as though it meant something. Dropping the team costs one row; guessing wrong corrupts the record. The no-history guard exists because its failure mode is silent rather than loud: the operator would see a clean run and never learn the override was inert. | (A) SKIP verified across 'SKIP', 'skip', '  Skip  ' -- all three omit ATL from the dict (len 31 with one skip) and emit source='skip' with a null qb_id. Downstream with the real 2-SKIP file: starters dict 30 teams, summary still 32 rows (every team visible), run_week -> 30 rows with neither skipped team present, saved as 2026_w01_qb_pass_yards_step69_skip.parquet. Grader verified on a short log via a redirected PREDICTIONS_DIR (no canonical log touched): a 25-row 2025 wk8 log with IND removed graded cleanly -- 25 rows, GRADED=21 / WRONG_STARTER=2 / NO_STATS=2, starter accuracy 21/23, MAE 52.62, quantile-bucket table intact, dropped team absent from the grades. No 32-team assumption found in evaluate.py, engineer.py or predict.py. (B) Fernando Mendoza (LV, 2026 rookie, 0 qb_matrix rows) raises with the full message; unknown name 'Patrik Mahomes' and ambiguous name 'Mark Miller' (2 gsis_ids: MIL411150 1987, MIL419373 1978-1980) both still raise. (C) All six non-SKIP names resolve to a unique gsis_id, all ACT, all last_season=2026, all with latest_team == their override team; career qb_matrix depth Mahomes 93 / Geno Smith 68 / Daniel Jones 58 / Bo Nix 36 / Jayden Daniels 21 / MALIK WILLIS 6 (1 row in 2025) -- Willis is by far the thinnest history on the board and his prediction should be read accordingly. (D) 2026 wk1 table: 24 default / 6 override / 2 skip, board 30 rows, and ZERO starters whose latest_team differs from their team (was 3 before the overrides). (E) test_prediction_features, test_predict, test_calibrate, test_rolling_features all pass. LATENT DEFECT FOUND AND FLAGGED, NOT FIXED: SKIP is only reliable for a FUTURE week. build_prediction_features falls back to schedules' home_qb_id/away_qb_id for any team absent from the starters dict; for 2026 wk1 those are null 16/16, so skipped rows carry qb_id=NaN and are dropped by preprocess_qb_matrix (which is WHY run_week returns 30). For a historical week those columns are populated (2025 wk8 and 2024 wk8 both 0 nulls), so a SKIP row would silently fall back to the schedule's QB and the team would be predicted anyway. build_prediction_features also returns 32 rows regardless, so a direct caller sees the skipped teams. Documented in RUNBOOK §5.2a with a warning not to rely on SKIP in a backfill or replay. **LATENT DEFECT FIXED (step 70).** SKIP is now explicit rather than incidental. `build_prediction_features` gained a `skip_teams` parameter and drops those teams from target_games BEFORE the schedules fallback, so a skipped team can no longer be resurrected from schedules' own home_qb_id/away_qb_id; `run_week` reads the skip list off the summary (`source == "skip"`) and passes it through. Reading the summary was chosen over a third return value because both existing callers (run_week, player_resolver.build_candidate_pool) unpack a 2-tuple -- the resolve_starters signature is unchanged, verified still returning a 2-tuple. PROVEN ON THE BROKEN CASE (2025 wk8, a completed week where schedules carries home_qb_id/away_qb_id with 0 nulls), run against a redirected PREDICTIONS_DIR so no canonical file was touched: with a SKIP row for KC, resolve_starters correctly omitted KC from the dict (31 teams), but BEFORE the fix build_prediction_features still emitted 26 rows including KC, silently resurrected as qb_id=00-0033873 Patrick Mahomes with fully-populated features (passing_yards_l8=257.125) and surviving preprocessing -- KC would have been predicted despite the SKIP. AFTER the fix: 25 rows, KC absent before and after preprocessing, KC's opponent still present. 2026 wk1 regression: build_prediction_features now returns 30 rows with skip_teams (was 32, with ATL and LV present), 30 after preprocessing, and run_week still returns 30 with neither skipped team present -- the same outcome as step 69 but now by construction rather than by the accident of null schedule qb_ids. build_candidate_pool unaffected (442 players, 30 QBs, no skipped teams; its own dropna already discarded the null skip rows). test_prediction_features / test_predict / test_calibrate / test_rolling_features all pass. RUNBOOK §5.2a warning replaced with the fixed behaviour. |
| 2026-09-01 | Models | run_week_receiving's saved log now carries `targets_l8` alongside `team_implied_total` and `as_of`, mirroring the step-53 `carries_l8` addition to the rushing log. Log metadata only -- targets_l8 is ALREADY a model FEATURE_COL (it comes from RECEIVING_ROLLING_COLS via train_receiving.ROLLING_COLS, confirmed at 38/38 FEATURE_COLS), so this changes the output column set only: no retraining, no artifact rebuild, no feature-frame change. Consumer update: the read-only dashboard's roster-staleness section now filters receiving rows on targets_l8 >= 3 instead of the "has a posted line" fallback, and reports its slice_basis per row so a pre-step-74 log is visibly on the fallback. | targets_l8 >= 3 is the prop-slice selector the receiving layer was fitted on (build_receiving_prob_artifacts.py) and is graded against (evaluate_receiving.PROP_MIN_TARGETS_L8); without it in the log, a live prediction file could not be filtered to the priced population without rejoining receiving_matrix, which defeats the point of an immutable standalone log. The asymmetry with rushing was found by the step-73 dashboard, which had to substitute "has a posted line" -- a materially different population (15 priced rows vs 141 prop-slice rows on 2026 wk1). | scripts/test_receiving_prediction_features.py 3/3 still pass unchanged (the feature frame was not touched -- only the runner's output selection -- so test (c)'s "column set == receiving_matrix + {as_of}" assertion still holds); 2026 wk1 re-run under label wk1_current: targets_l8 non-null 412/412, min/median/max 0.00/1.75/12.62, prop slice 141/412 rows, 14 of the 15 posted lines inside the slice |
| 2026-09-01 | Ingestion | lines_to_overrides(consensus_df, matched_events, season, week) -- season and week are now REQUIRED and the function filters to them itself, raising ValueError rather than ever spanning weeks. Three guards: (1) matched_events lacking season/week COLUMNS raises (week identity unverifiable -> refuse, don't guess); (2) an empty (season, week) slice raises, naming the weeks actually present; (3) a team appearing in more than one surviving game raises. Events with NaN season/week (no schedule match) are dropped with a printed count. The three scripts/test_2026wk1_*.py call sites now pass the full matched frame + (2026, 1) instead of pre-filtering; their assertions are untouched. | THE FAILURE MODE IS THE POINT: the returned dict is keyed by TEAM, so a multi-week frame produces no error and no NaN -- each team just keeps whichever of its games iterated last, putting a wrong team_implied_total (a model FEATURE) on every predicted row, invisibly. Saved game-lines snapshots span the whole season (272 events / 18 weeks), so the unfiltered frame is the natural thing to pass. Step 74 hit exactly this; step 62 escaped only because it filtered after a live fetch_events(). Guarding on 'input spans >1 week' ALONE would not have caught step 74, whose frame had no season/week columns at all -- hence guard (1), the load-bearing one. | Demonstration on the saved 2026 wk1 snapshot, no API calls: unfiltered 272-event frame previously yielded a plausible-looking 32-team dict in which 32/32 teams were WRONG (e.g. ARI implied total 18.00 vs correct 28.50, LAC 21.50 vs 28.50); step-74's bare event frame now raises on missing columns; wk 9 against a wk1-only frame raises; a duplicated team raises naming ['NE','SEA']; self-filtered dict == pre-filtered dict == step-74 dict (32 teams, exact equality); 2026 wk1 re-run through the guarded path reproduces all three logs EXACTLY (receiving 412x20, rushing 111x21, QB 30x16, assert_frame_equal passes modulo generated_at_utc). test_2026wk1_receiving_predictions.py and test_2026wk1_rushing_predictions.py pass. PRE-EXISTING, NOT INTRODUCED: test_2026wk1_lines_to_predictions.py still exits 1 on FileExistsError because it alone omits force=True on run_week; it clears lines_to_overrides first (272 -> 16) |
